class Solution {
    public void rotate(int[] nums, int k) {
        int[] nume = new int[nums.length];
        int index;
        for (int i = 0; i<nums.length; i++){
            index = (i+k)%(nums.length);
            nume[index]=nums[i];
        }
        for (int i = 0; i<nums.length; i++){
            nums[i] = nume[i];
        }
    }
}
